{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <input type="checkbox" id="accept_cgu" name="_accept_cgu"  value="on" required="required"/>
        <label for="accept_cgu">J’accepte les <a href="{{ path('se_layout_cgu') }}">conditions générales d'utilisation</a> de Service Enchère.</label>
    </div>
    <div class="text-center">
        <input type="submit" value="{{ 'registration.submit'|trans }}" class="btn btn-primary" />
    </div>
{{ form_end(form) }}

