
    <form id="form_recherche" action="" method="get">
        <div class="form-group">
            <div class="row">
                <div class="col-md-4 filter">
                        <select class="form-control"
                                name="advert"
                                id="advert_id" >
                            <option value='0' >Tous les avis</option>
                            <optgroup label="Avis reçus">
                              {% for advert in listAdvertUser %}
                                  <option {{ app.request.query.get('advert')== advert.id ? ' selected ' : ' ' }}
                                      value={{ advert.id }}>{{ advert.title }}
                                  </option>
                              {% endfor %}
                          </optgroup>
                            <optgroup label="Avis envoyés">
                            {% for advert in listAdvertComment %}
                                <option {{ app.request.query.get('advert')== advert.id ? ' selected ' : ' ' }}
                                    value={{ advert.id }}>{{ advert.title }}
                                </option>
                            {% endfor %}
                            </optgroup>
                        </select>
                </div>

                <div class="col-md-4 filter">
                    <input class="btn btnIcon-search" type="submit" value="Afficher"  />
                </div>
                <div class="col-md-4 filter">
                  <a href="{{ path('se_platform_comment_user_list') }}"
                            type="button" class="btn btnIcon-search" >
                             Réinitialiser
                  </a>
                </div>
              </div>
        </div>
    </form>
