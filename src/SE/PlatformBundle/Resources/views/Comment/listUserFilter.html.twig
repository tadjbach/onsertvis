
    <form id="form_recherche" action="" method="get">
        <div class="form-group">
            <div class="row">
                <div class="col-md-4 filter">
                        <select class="form-control"
                                name="state"
                                id="state_id" >
                            <option value='0' >Tous les avis</option>
                              {% for state in listCommentState %}
                                  <option {{ app.request.query.get('state')== state.id ? ' selected ' : ' ' }}
                                      value={{ state.id }}>{{ state.labelNormal }}
                                  </option>
                              {% endfor %}
                        </select>
                </div>

                <div class="col-md-4 filter">
                    <input id="filter_normal" class="btn btn-primary" type="submit" value="Afficher"  />
                </div>
                <div class="col-md-4 filter">
                  <a href="{{ path('se_platform_comment_user_list') }}"
                            type="button" id="filter_normal" class="btn btn-primary" >
                             Réinitialiser
                  </a>
                </div>
              </div>
        </div>
    </form>
