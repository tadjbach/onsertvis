  <h4 class="text-left"><p class="trunc">Posté par {{ advert.user }}</p></h4>
  <hr class="hidden-xs hidden-sm">
  <p>Membre depuis : {{ user.dateCreation|date('d/m/Y') }}</p>
  <p>Tél :
     {% if user.isDetailVisible or is_granted('ROLE_SUPER_ADMIN')%}
     {{ user.phoneNumber|split('', 2)|join(' ') }}</p>
     {% else %}
       0X XX XX XX XX
     {% endif %}
</p>
<a href="{{ path('se_platform_user_view', {'userId': advert.user.id}) }}" class="btn btn-success btn-block">Voir le profil</a>
